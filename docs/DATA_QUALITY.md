# Data Quality

- Checks básicos no DAG (row count, nulos, ds match, observações).
- Validação estilo Great Expectations (leve, sem projeto GE completo).
- Reports em `include/quality/...` e falha do DAG se reprovar.